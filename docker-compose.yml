version: '3.6'

services:
  db:
    image: postgres
    privileged: true
    ports:
      - "9501:5432"
    environment:
      POSTGRES_USER: johnk
      POSTGRES_PASSWORD: johnk123
      POSTGRES_DB: vps_test
  adminer:
    image: adminer:4.7.8-standalone
    restart: always
    ports:
      - "8080:8080"
    depends_on:
      - db


